export interface OrderInfo {
    fullName: string;
    phone: string;
    address: string;
    district: string;
    ward: string;
    addressDetail: string;
    note?: string;
    images: string[];
}